import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Removed deprecated experimental.appDir (App Router is stable by default)
}

export default nextConfig
